use TerranBarracks as actionspec.
use TerranBarracks as knowledge.
exit = never.

%if train is believed then do action train marine
module TerranBarracks {
	if bel(train) then train("Terran Marine") + delete(train).
}